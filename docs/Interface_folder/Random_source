class UniformIntRandom;
  It is a base class used to Generate random numbers
  You should override all of its member function

  virtual const int32_t rand(const int32_t& from, const int32_t& to) = 0;
    It should be implemented to return a number which is not less than $from$ and no greater than $to$.
  
  virtual void set_seed(const int32_t& seed) = 0;
    As its name, set a random seed.

  // In file "src/main.cc", I implement a child class named DefaultUIRandom.
  // It Generate uniform distribution random number.